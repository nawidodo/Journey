# Recommendations — Systems Security Journey

All advice in one place. Applied items marked ✅, open items have a "when" trigger.

## Applied (Aug 2026)
- ✅ **FUZZ-COURSE.md added**: zero-knowledge gated FZ0–FZ12 — own coverage-guided fuzzer (libFuzzer/AFL-class) finding REAL bugs in YOUR code: CGF loop reference-run + harness → own edge-coverage bitmap via sanitizer-coverage callbacks (edge-vs-line rationale) → mutation engine (deterministic+havoc, dictionary, validity/diversity tradeoff table) → main loop w/ queue/energy/coverage-replay sanity + growth curve → crash triage (stack dedup, own delta-debug minimization, ASan reports, report file) → structure-aware mutators (token-level, length-fields) with dumb-vs-smart comparison → seeds+dictionary+could-minimizer A/B (head-start quantified) → sanitizers (ASan/UBSan matrix, sanitizer-only bug found) → real hunt on an earlier parser (artifact set + fix + regression test) → parallel workers scaling curve + benchmark honesty + limits → AF/OSS-Fuzz reading + CVE case-study maps + adopt/reject table → researcher wrap (README, CI-lite, crash queue, coverage-blind-spot essay) → cold capstone (fresh parser found/minimized/fixed cold). Scope: own code + authorized targets only. Charts step 05-12.
- Flagship set now: 41 — vulnerability-discovery engine line complete (fuzz→CVE-study→kernel/browser courses fed).
- ✅ **SUPPLY-CHAIN-COURSE.md added**: zero-knowledge gated SC0–SC12 — own apt/brew/choco-class package manager + registry + supply-chain security: four-machine anatomy (format/repo/resolver/verifier) + ecosystem table → own format (archive+manifest+hashes, builder+validator) → repo server (index versioning, publish, deprecation) → resolver (constraints, backtracking-lite, lockfile byte-reproducible, cycle/conflict tests) → verification (package+index signing, tamper battery, key-bootstrapping essay) → transactional install engine (stage/apply/rollback, inject-failure rollback, scripts-risk note) → from-source building + honest reproducibility report → cache/offline/mirror + cache-poisoning defense → supply-chain attack catalog (typosquatting/confusion/malicious-update, planted-malicious-update rejected by policy + pinning essay) → SBOM generator + CVE audit (planted vuln flagged, remediation shown) → real ecosystems reading (deb/brew/OCI) + Sigstore keyless essay → spkg CLI + spkg-repo admin + fresh-sandbox end-to-end → cold capstone (publish→resolve→verify→install→audit, tamper rejected). Ethics: own registry only, attacks reading+defense never against third parties. Charts steps 24-39/75/91.
- Flagship set now: 40 — software-supply-chain line complete.
- ✅ **MDM-COURSE.md added**: zero-knowledge gated MD0–MD12 — own mobile-device-management (Android Device-Owner LIVE on emulators + iOS fence reading): custody model + can/can't-see boundary table → enrollment (DPC + dpm set-device-owner, persisted; iOS APNs/profile reading) → policy engine (document+version, push/poll, camera-disable applied + rollback) → groups/assignment + managed-config + revocation lifecycle → compliance checks (root detection, app allowlist, encryption, policy-match) w/ scoring + grace + planted violation → app catalog (hash-verified install/update/remove) → remote actions LOCK/WIPE under rituals (snapshot-first, confirm-step, audited; corporate-wipe selective) → zero-touch onboarding reading + theft-blocking analysis → MANDATORY privacy unit (BYOD/COPE matrix, capability-vs-legitimacy essay, mini-mization answer, operator guardrails; gate-before-actions) → protect-the-protector (TLS, token+nonce replay rejection, server hash-chain, threat model) → real-world reading (Intune/Jamf, UEM convergence, anti-MDM criticism answered) → mdm-lite console (fleet posture/policy editor/ritual actions/audit log, 2-emulator demo w/ planted violation) → cold capstone (fresh emulator full custody loop + ritual + audit). Fiduciary line: emulator fleet only, lab-only wipe, MD8 before MD6.
- Flagship set now: 39 — device-custody line complete; Android lane fully wired.
- ✅ **DLP-COURSE.md added**: zero-knowledge gated DL0–DL12 — own data-loss-prevention (endpoint clipboard/USB + network egress + storage-at-rest, privacy-constitution first): lane map + content taxonomy + own Luhn CCN seed → content-classifier core (regex+Luhn+dictionary+context scoring + near-miss analysis) → policy engine (rule format, AUDIT/QUARANTINE/BLOCK action bus, dry-run vs armed) → endpoint lane (clipboard monitor cross-OS + eBPF USB-write block) → network lane from PROXY-MITM skills (SMTP/HTTP classify + TLS blind-spot honesty + egress analytics) → storage lane (file walker + cache + quarantine vault + review) → cloud/mobile frontier reading (MAM/MDM, honest walls) → PRIVACY balance unit (law, notice design, minimization, misuse-vector review, chilling-effect essay) → evasion lab (zip/base64/rot misses table + DLP-as-deterrent position) → incident workflow (severity/queues/playbooks + metrics + FP feedback loop) → dlp-lite CLI + integrated 3-lane leak scenario → vendors reading + DLP-in-SIEM design + gap essay → cold capstone (fresh unseen leak caught). Privacy: own-lab data, match-evidence only, notice-first, no surveillance product.
- Flagship set now: 38 — data-protection line complete; content lanes wired from proxy/USB/BD skills.
- ✅ **SIEM-COURSE.md added**: zero-knowledge gated SI0–SI12 — own log-SIEM (collect→parse→index→query→correlate→alert→dashboard→report, compliance-driven, data-discipline: generated/lab logs only): anatomy + compliance reading + fake-org log generator w/ planted incidents → syslog listeners (RFC 5424) + file-tail + ingestion-health view → grok-lite parsers for 4 formats + schema mapper + parse-error report → day-shard store + inverted index + own query syntax + retention policy → time-window correlation rules (5-rule set; planted fires, background silent, throttling) → alert manager (severity/dedup/webhook-to-own-receiver/evidence threads) → dashboard module (6 widgets render, incident-day story) → hash-chained event integrity + tamper detection + compliance report generator → honest throughput/query-latency curves + humility table vs Elastic/Splunk + verdict → SOC workflow (triage→case→after-action rule feedback, 3-alert shift) → market reading (Splunk/QRadar/Elastic + SIEM-dead-debate essay + overload cautionary tale) → siem-lite CLI + SOC-week simulation (3 planted incidents caught) → cold capstone (fresh unseen week caught end-to-end + report). Positioned 4th in the detection ladder: EDR→Behavior→XDR→SIEM.
- Flagship set now: 37 — log-analytics + compliance layer complete.
- ✅ **DETECTION-LADDER.md added**: merge-map for EDR/Behavior/XDR — order: EDR (sensor+schema+single-host detection) → BEHAVIOR (verb-stream inference, or DETECTION as orthogonal fork) → XDR (multi-source correlation + response + SOC); each assumes prior, separate cold capstones per course = three proof points; full rationale + parallel optional tracks (INTEGRITY/ROOTKIT-DEFENSE).
- ✅ **BEHAVIOR-DETECTION-COURSE.md added**: zero-knowledge gated BD0–BD11 — own "suspicious user behavior" detector (command-verb watch: whoami/ping-recon/sequences, SOC-framed, consent-ethic): verb taxonomy + MITRE ATT&CK map (T1016/T1033/T1059/T1071) + ethics preamble → Linux exec collector (eBPF/auditd, session attribution) → classifier w/ canonicalization + unknown bucket (20-command battery) → stateful session sequence engine (recon walk fires; benign sandwich silent) → per-user baseline profiles + deviation (novel-action flagged, cold-start doc) → severity/dedup/suppression/case-stub alert model (3-alert demo) → FP measurement report + cost-matrix essay + tuning pass → OS map (Windows ETW/Sysmon, macOS ES wall, mobile OS-gated) → evasion shapes (PowerShell -enc/base64/renamed-binaries, shape-not-name essay) → watch-cli live shift + SOC notebook → ATT&CK coverage + UEBA reading + monitoring-law précis → cold capstone (rebuild, 2 planted signals caught in live shift). Ethics: own lab sessions, simulated users, no deployment.
- Flagship set now: 36 — behavioral detection specialization complete (EDR→XDR→BEHAVIOR triangle).
- ✅ **XDR-COURSE.md added**: zero-knowledge gated XD0–XD12 — own cross-source detection-and-response platform (EDR-COURSE's big sibling, defensive, response-with-safety hardline): stack map + organ inventory + two-source join hello → own JSON event schema w/ validator+versioning → 2 collectors from prior skills (endpoint + network flow/DNS) → pipeline normalize/enrich/store/query API + honest drop-stats → rule engine + cross-source correlation rule + baseline profiler (lab NETEXEC/PROXY/INSTRUMENT tools fire rules) → SAFE response module (dry-run/allowlist/audited/reversible) + playbook + tabletop → SOC lifecycle (alert→triage→case) + 3-alert IR shift → threat-intel module (IOC lifecycle + lagging-indicator essay + FP curation) → network X (DNS analytics/SNI metadata/beaconing) → mobile+cloud extend (app schema events, CloudTrail-grade reading, login analytics, scope-creep essay) → protect-the-protector (pipeline integrity/sensor health/DETECTION-suite on own sensor) → real-XDR reading (CrowdStrike/SentinelOne/Defender vs yours + hype-vs-reality essay) → cold capstone (xdr-lite catches live lab incident: collect→detect→respond→case). Safety: dry-run-first, own-lab only, no real victims.
- Flagship set now: 35 — detection-response enterprise line complete; the repo's organs become one body.
- ✅ **DETECTION-COURSE.md added**: zero-knowledge gated DT0–DT12 — cross-OS debugger/hook DETECTION suite (blue-side, own apps; evasion dual-use analyzed reading-only): signal taxonomy × layer map → Linux (TracerPid/TRACEME/maps/timing; gdb+strace caught) → macOS (P_TRACED/PT_DENY_ATTACH/task_for_pid; lldb caught) → Windows (PEB/NtQuery DebugPort/DR-registers/timing; x64dbg caught) → uniform hook-scan (inline-bytes/IAT-vs-disk/call-stack; own-hook round-trip) → kernel receipts (dmesg/sysmon//proc/modules) → Android (JDWP/TracerPid/Frida-signatures detection rules; emulator battery) → iOS (sysctl/deny-attach/jailbreak+Frida reading + compiling skeleton) → meta-layer detection-of-detection + randomization countermeasure-lite + dual-use essay → evasion-resistant design (correlation alarm + TPM/SE hardware assist + gap doc) → dxdetect suite (status/watch/verify + live battery vs real tools on 3 hosts) → VAC/EDR reading + driver-grown essay → cold capstone (rebuild core, live battery caught, gap doc). Line: protects own apps; detection≠evasion circuits never built.
- Flagship set now: 34 — detection line complete; INTEGRITY+INSTRUMENT+ROOTKIT+EDR fused into one spine.
- ✅ **INTEGRITY-COURSE.md added**: zero-knowledge gated IT0–IT12 — cross-OS app integrity protection + attestation (legit defensive; tamper-tests on OWN artifacts): layer map (static/runtime/attestation) + manifest verifier w/ tamper matrix → signing-chain notebook (4 OSes) + own CA/signer round-trip → Windows Authenticode (sign+verify+tamper evidence) → macOS codesign (ad-hoc→notarization + tamper fail) → Linux packages/IMA + trust essay → Android APK signing schemes + runtime self-check (own signature + hook-framework DETECTION reading) → iOS enforcement model + App Atestation reading + compiling skeleton (device ceiling honest) → runtime guard (self-hash/hook-scan/TracerPid + fail policy, 3 attack classes detected) → arms-race essay (client-side loses alone) → attestation protocol-lite (challenge-response + TPM PCRs + replay rejection) → intygrity suite (4-OS modules + TU battery + report cards) → cold capstone (sign→guard→attest fresh app + tamper battery). Boundary: own apps only, hook detection never bypass, no DRM-abuse.
- Flagship set now: 33 — integrity/attestation line complete across desktop+mobile.
- ✅ **VM-OBFUSCATION-COURSE.md added**: zero-knowledge gated VO0–VO12 — own VM-based obfuscator (VMProtect-class) with the deobfuscation twin REQUIRED per unit: "evasion" framing refused (protects YOUR software; deobfuscation built alongside): lift C function→your PL-VM bytecode (+generator, diff-clean) → control-flow flattening + constant encoding + opaque predicates (growth measured) → blob+loader+self-check → static forensics report (leaks found+fixed) → trace+dump recovery via OWN INSTRUMENT tool → auto-deobfuscator (fold/eliminate/CFG-rebuild→pseudo-C, honest misses) → armoring back (per-instance keys, opaque dispatch) + essays the never-ending loop → VMProtect/Themida + VM-packed-malware paper mapping → YARA/heuristic detector flagging OWN builds + FP-rate discipline → 400-word obfuscation≠security truth page → vmp-lite CLI + peer challenge (cross-deobfuscation blinds). Hardline: no AV evasion targeting, protect-your-software only.
- Flagship set now: 32 — software-protection + RE-recovery lane complete.
- ✅ **INSTRUMENT-COURSE.md added**: zero-knowledge gated DI0–DI12 — OWN Frida-class dynamic instrumentation framework: 4-layer anatomy + ptrace hello → ptrace+/proc/mem injection (regs restored) → C agent w/ your command protocol (read/write/call over socket) → LD_PRELOAD then OWN inline trampoline hooking (save-regs/trampoline/restore, one documented crash) → script layer w/ onEnter/onLeave + your interceptor.attach API → AMD64 ABI torture test (6+ mixed args, float/SSE) → own /proc-maps parser, memory scanner, ELF64 export parser, backtrace → anti-instrumentation DETECTION module (TracerPid/byte-scan/timing) + essay (not evasion-build) → Windows lane (CreateRemoteThread DLL agent + own x64 inline hook) → macOS lane reading (DYLD interpose/task_for_pid/codesign walls) → real-Frida deep-read + 3-vs-3 engineering comparison + ethics page → **frida-lite CLI** (attach/script/trace/scan/dump/bt) + authorized CTF trace → cold capstone (rebuild core, live session, ethics). Scope: own binaries + course CTF only.
- Flagship set now: 31 — dynamic-analysis lane complete (debugger→disasm→instrument).
- ✅ **USB-GAMEPAD-COURSE.md added**: zero-knowledge gated UG0–UG12 — own USB game controller that works on every major OS (device-side twin of USB-DRIVER): HID-class concept + descriptor-plan → hand-written report descriptor validated → tinyusb device stack + endpoint/bInterval + measured report rate → ADC sticks + own calibration/deadzone/curve → debounce + report assembly + host viewer → Linux joydev+your reader + **uinput virtual joystick (your PC becomes the pad)** → macOS IOKit reader + GCController mapping gap → Windows RawInput reader + XInput-wall demo + emulator reading → Android HID host app + reticle test → honest per-OS latency rig + table + optimizations → virtual-lane full matrix (uinput live; Windows ViGEm-style reading + signing wall; macOS ceiling) → extended descriptors (flight joystick/racing wheel/force-feedback/composite) → cold capstone (same board reads on 4 hosts + uinput game drive + latency rerun). Rules: measured numbers only, walls documented.
- Flagship set now: 30 — device+host+wires all one repo; hardware lane complete.
- ✅ **GPU-CRACKER-COURSE.md added**: zero-knowledge gated GC0–GC12 — own GPU password cracker Metal→CUDA (legit: own hashes/authorized recovery): SHA-256 vector-verified → 1M-parallel Metal kernel → mask attack → dictionary+rule engine (coverage-vs-brute table) → hash-resistance ladder (bcrypt/scrypt memory-hard, measured CPU times) → CUDA port (occupancy profiling) → NV cracker core + stream overlap → memory-hard frontier engineering (honest ceilings) → bench discipline (methodology + hashcat baseline comparison) → multi-stream/multi-GPU scaling → scope-gated CLI + authorized-only playbook + target-ethics essay → hashcat design-review → cold capstone gpcrack v1 (live authorized drive + honest bench). Target-line: own hashes only, scope-gate armed, hashcat read-only reference.
- Flagship set now: 29 — GPU security line complete (compute U → cracker GC).
- ✅ **ROOTKIT-DEFENSE-COURSE.md added**: rootkit/bootkit request reframed honestly — full MECHANICS minus evasion + complete defensive twin: taxonomy×detection map + line-page → non-evading self-announcing syscall interposition + callbacks (visible, removable) → hooking mechanics (kprobes/ETW) re-created announced + hook-inventory detector → DKOM-class manipulation SAFE form (annotation, no hiding) + list-walker → integrity/hook audit suite + baseline discipline → boot chain map + Secure Boot on/off + TPM PCR extend/quote → LoJax/BlackLotus read-only analysis mapped to boot-chain → own measured-boot verifier (lab alteration flagged) → integrated mini-anti-rootkit suite (0 clean / 4 lab findings) → 400-word evasion-regulation essay → cold capstone (interpose→audit→report). Hardline: no hiding code, no persistence, no artifacts; real malware reading-only. Prereqs: DESKTOP W1–W7.
- Flagship set now: 28 — every sensitive-by-request domain carried across the line intact (kernel, browser, input, rootkit) with its defensive twin.
- ✅ **INPUT-SECURITY-COURSE.md added**: zero-knowledge gated IS0–IS10 — input-pipeline mastery WITHOUT stealth (the "stealth keylogger" request reframed honestly): pipeline maps (Linux evdev / macOS IOHID+TCC / Windows hooks+raw input) → transparent listeners on all three OSes (visible, timed, no persistence, consent-note auto-generated, constitution written unit-0) → layout/dead-key decoder → the DETECTION twin (Windows hook-enum + Defender findings, macOS TCC-grant audit, Linux /dev/input consumer scan) catching your OWN listeners → read-only real-keylogger analysis mapped to pipeline attach-points → 400-word consent essay + guardrails → cold capstone (capture→detect→report round-trip). Hardline: own devices/keys only, no evasion, no persistence; AV-flag = lesson.
- Flagship set now: 27 — input layer OK, both sides of the table.
- ✅ **VPN-ALLOS-COURSE.md added**: followup gated VA0–VA8 — your WireGuard-lite protocol wired into every major OS pipe: tunnel-matrix + PacketPipe abstraction spec → Linux/FreeBSD tun live refresh → Windows **wintun** (ring adapter, signed driver, userspace dll) live protocol → macOS utun deep (mac↔Linux VPN) → Android VpnService app (emulator routes through your protocol) → iOS NetworkExtension reading + compiling skeleton + honest entitlement ceiling → one interface/five dialects refactor + 3-OS live mesh → per-OS routing/MTU matrix + survival guide → cold capstone (new pipe written from spec, mixed-OS mesh VPN running). Preq: VPN VP0–VP12.
- Flagship set now: 26 — protocol once, pipes everywhere; network-security line complete.
- ✅ **VPN-COURSE.md added**: zero-knowledge gated VP0–VP12 — WireGuard-lite secure tunnel protocol, hand-designed + hand-built: tun interface (utun//dev/net/tun) raw-tunnel ping → RFC-style framing spec → UDP transport state machine + TCP-over-TCP collapse reading → ChaCha20+Poly1305 hand-rolled (RFC 8439 vectors) + X25519/HKDF (RFC 7748/5869 vectors) → 3-message handshake + per-direction keys + replay rules → complete protocol spec+code audit 1:1 → routing/NAT/MTU-minefield → keepalive/dead-peer/rekey/roaming (live tests) → honest overhead+crypto-vs-openssl bench → threat model + 2 hardening fixes (rate-limit/cookie-lite) → WireGuard-vs-OpenVPN design-diff paper → cold capstone (core rebuilt; two endpoints one network; spec+threat model as designer's). Lab-only + educational-crypto-only honesty rule.
- Flagship set now: 25 — cryptography+crypto-apps line: TLS, e2ee, side channels, VPN all own-hand.
- ✅ **PROXY-MITM-COURSE.md added**: zero-knowledge gated PM0–PM12 — Proxyman-class sniffer+MITM: packet capture/parse/stream-reassemble vs tcpdump → 5-tuple flow table + transaction extraction + own filter engine → HTTP forward proxy + CONNECT tunnel → own CA + per-host certs + TLS MITM showing plaintext (SSLKEYLOGFILE proven) → decode/edit/replay/rewrite → WebSocket frames + chunked/SSE streaming → cert-pinning honest wall (bypass = others'-devices tooling, out by design) → map-local/inject/redirect/pause → throttle (token bucket) + big-body streaming + overhead bench → QUIC/gRPC/DNS frontier (gRPC frame hand-parse) → 300-word boundary essay (legit vs surveillance, guardrails) → cold capstone (capture+CONNECT+TLS core rebuilt; full own-lab session end-to-end). Hardline: your traffic only, CA into own tools only.
- Flagship set now: 24 — network-tooling line complete (sniffer→proxy→protocols), each own-hand built.
- ✅ **NETWORK-DRIVER-ALLOS-COURSE.md added**: followup gated OSN0–OSN8 — Linux ND-driver skeleton re-spelled per major OS: VM farm (Linux/FreeBSD/Windows-eval) + 4-column stack map → FreeBSD ifnet driver written + ping-proven (second full dialect) → Windows NDIS 6 miniport skeleton + NBL flow (signing=honest ceiling) → macOS IONetworkFamily DER-study + compiling skeleton (license-honest reading lane) → one-model-many-APIs ring/NAPI→taskqueue/DPC/workloop table → envelope (skb/mbuf/NBL) + offload hook matrix + mbuf checksum test → honest cross-OS bench + bottleneck essays → per-OS filter-point/trust table (WFP/NEF/BPF/XDP) → cold capstone (FreeBSD driver re-attached cold). Preq: NETWORK-DRIVER ND0–ND12.
- Flagship set now: 23 — every major OS's network-driver dialect mapped, one proven twice.
- ✅ **USB-DRIVER-COURSE.md added**: zero-knowledge gated US0–US12 — own USB driver on QEMU's virtual bus (real USB stack, zero hardware): model+descriptor-tree map → raw descriptor dumper vs lsusb → Linux usb_driver probe/ownership → URB control+bulk transfers w/ usbmon proof → lifecycle incl. LIVE hot-unplug mid-transfer → one protocol class end-to-end (serial or HID) → IOKit mapping + compiling client skeleton (ceiling documented) → KMDF skeleton + winusb round-trip in Windows guest → Android gadget-mode reading/lite → USB-security lens + attach-policy enforcement live → DMA/timeout/isoc + honest throughput bench → udev/node/pipeline doc → cold capstone (byte round-trip + OS matrix + maintainer writeup). Prereqs DRIVER V0–V6.
- Flagship set now: 22 — the full device-driver ladder (edu→netdev→USB) plus every major OS lane mapped.
- ✅ **NETWORK-DRIVER-COURSE.md added**: zero-knowledge gated ND0–ND12 — own NIC driver on QEMU virtio-net: packet-lifecycle map → ethernet framing parser → virtqueue/DMA ring setup → TX xmit+kick (ping proven) → RX pre-fill + NAPI receive (bidirectional) → NAPI budget + CPU/pps measurement → checksum offload negotiation + software fallback → driver-line IP/TCP hand-parse → in-driver RX filter (drop before stack, proven) → TX batching vs per-packet kick → honest bench vs stock virtio_net + bottleneck essay → real-NIC reading (ixgbe mapping, SR-IOV/RDMA divergence) → closed capstone (cold TX+RX+NAPI rebuild, iperf3 through your driver, maintainer's writeup). Driver-floor V0–V6 + K-units prereqs; own-VM lab.
- Flagship set now: 21 — device, network-driver, and kernel lines all hand-built.
- ✅ **BROWSER-EXPLOIT-COURSE.md added**: zero-knowledge-audited gated BE0–BE12 — browser-exploit re-creation on YOUR OWN JS engine (from INTERPRETER/PL): memory-model/Value layouts + forged tags → planted array-OOB → addrof/fakeobj primitives + code-pointer read → planted JIT type-confusion → string-len + GC-UAF double-free → fuzzing your engine (libFuzzer/afl) → sandbox-lite seccomp + choke-point map → in-the-wild chain READING (BlastDoor/FORCEDENTRY) mapped stage-to-unit → sanctioned CTF builds air-gapped OR honest ceiling doc → own behavior-monitor catching own exploits (Sigma-lite) → fixing+regression side → cold capstone (planted random bug found+exploited+ZDI-report+ethics). Hardline: own engine/own VM/own data only; real browsers never targets; nothing ships; 0-day beyond scope.
- Zero-knowledge flagship set now: 20 (18 courses + CVE study + this) — browser = the last major domain standing, now covered.
- ✅ **CVE-STUDY-CATALOG.md + CVE-STUDY-COURSE.md added**: top-5 real CVEs per major OS (Linux/Windows/macOS/Android) as READING + class-re-creation study — patch-first discipline, class mapping to X/K/N/A lab units, trigger-shapes in own VMs only, verified NVD-before-citation marks. KV0–KV7 gated study course: catalog audit → per-OS deep reads → master class matrix → cold bulletin triage → capstone (matrix + triage + new class re-creation + researcher-format writeup). Explicit ethics gate: no real PoC ever compiled/run; weaponized exploitation out of scope by design (the KERNEL-EXPLOIT X0/X12 line).
- Zero-knowledge course set now: 19 total course/docs floors (18 courses + CVE study track).
- ✅ **KERNEL-EXPLOIT-COURSE.md added**: zero-knowledge-audited gated X0–X12 — kernel exploit re-creation on YOUR OWN vulnerable-by-design LKM in YOUR OWN QEMU VM: memory-map/taxonomy → planted stack overflow → first RIP control (ret2function) → SMEP/SMAP/KASLR wall + own ROP chain → heap UAF + kmalloc groom → race/TOCTOU win-rate tuning → eBPF-verifier-class deep-reading + OOB-leak class re-creation → full mitigation matrix + KASLR-leak/SMEP-ROP bypasses → real-CVE root-cause papers (no PoCs) → escalating-defense ladder → two-class chained engagement + self-detection → cold-rebuild capstone with ZDI-style writeup + disclosure/ethics page. Hardline: own VM, own planted bugs, lab flag file, nothing ships, 0-days explicitly career-research beyond course scope.
- Zero-knowledge course set now: 18 — the exploit-research ladder (re-creation, not weaponization) joins Linux/Windows/Desktop/jailbreak lanes.
- ✅ **DATABASE-COURSE.md added**: zero-knowledge gated Q0–Q12 SQLite-class SQL database — file pages/slotted layout/free-list → own B+tree (fan-out benchmark vs scan) → SQL tokenizer+recursive-descent parser → query planner/executor w/ EXPLAIN → WAL transactions + kill -9 crash matrix → concurrency (locks or MVCC + lost-update test) → joins (nl/hash) + GROUP BY → secondary indexes + PK/UNIQUE/FK constraints → NULL 3-valued logic + IN/CASE/LIMIT/u-funcs → sqlite3-style CLI + prepare/step/finalize C API (embeddable by sscript) → backup/integrity-checker/VACUUM → closed capstone (qb v1, 1M rows, cold B-tree+query rebuild, DB-engineer post-mortem).
- Zero-knowledge course set now: 17 (…+Database) — storage+SQL line completes the PL/LLVM/Interpreter compound: languages, engines, databases all written by hand.
- ✅ **INTERPRETER-COURSE.md added**: zero-knowledge gated I0–I12 — embedded scripting interpreter, the Lua position: tagged Value union → sscript lexer/parser → env-walking evaluator with stack-trace errors → tables+metamethods → closures/varargs/multi-return → string interning + first C exports → value-stack C API (open/load/call/register) → baked-in debugger (line hooks/breakpoints/backtrace) → pcall + traceback + error objects → mark-sweep GC with host-ref rule → honest interpreter-slowdown benchmark → closed capstone (sscript embeds into a real host program — GE engine or editor — cold evaluator rebuild). Distinct from PL (compiler chain) and LLVM (native backend): covers script-into-C.
- ✅ **LLVM-LANGUAGE-COURSE.md added**: zero-knowledge gated LL0–LL12 — read/write IR by hand (SSA, phi) → own lexer/parser (lango) → AST→IR codegen (IRBuilder) → variables/functions via alloca+mem2reg → control flow + phi loops → type checker → opt -O2 pass tour → closures (fn-ptr+env struct) → strings+own C runtime → verifier/IR-dump/golden tests → ORC JIT REPL → closed-notes capstone (lango v1 → native binaries + JIT, two real programs, engineer post-mortem). Professional architecture: frontend by you, backend borrowed — the Rust/Swift shape.
- ✅ **PROGRAMMING-LANGUAGE-COURSE.md added**: zero-knowledge gated PL0–PL12 — REPL/pipeline seed → hand-written lexer → recursive-descent parser → tree-walking interpreter w/ scopes → closures-upvalues → type checker → own mark-sweep GC → bytecode VM + disassembler → JIT reading/sketch → strings/errors/modules/stdlib → REPL+golden tests+formatter → AST→C transpiler afternoon → closed-notes capstone (mylang v1 runs programs you wrote in it, VM rebuilt cold, designer's post-mortem).
- Zero-knowledge course set now: 14 (Linux/Apple/Android/Windows/Malware/GPU/Low-Level/Driver/EDR/Auth-Tooling/Potato/NetExec/Game-Engine/Programming-Language) — every major “write your own X” domain covered, each gated and capstoned.
- ✅ **GAME-ENGINE-COURSE.md added**: zero-knowledge gated GE0–GE12 — game loop + timestep math → input/sprites/AABB 2D shell → own ECS → software raster (own matrices) → Metal renderer port → 3D math/camera/z-buffer → OBJ loader + lighting → live physics-lite (gravity/jump/one-way platforms) → event audio → asset formats + dev console → authoritative UDP netcode-lite → closed-notes capstone (engine ships a 10-minute game at 60fps + engineer post-mortem). Fun-but-real track built on GPU-BEGINNER-COURSE.
- Zero-knowledge course set now: 13 (Linux/Apple/Android/Windows/Malware/GPU/Low-Level/Driver/EDR/Auth-Tooling/Potato/NetExec/Game-Engine).
- ✅ **NETEXEC-COURSE.md added**: zero-knowledge gated NE0–NE12 — UX/CLI spec → credential-format parser → own SMB tri-state auth engine (reusing AUTH-TOOLING wires) → SAMR/MSRPC/LDAP enumeration → own WinRM exec → 4-transport dispatcher → LDAP user/SPN diet → lockout-aware sprayed safe defaults → own plugin/module system → targets/JSON/exit-code finish → Sigma self-detection → scope-refusal ops ethics → closed-notes capstone (full sweep from clean VM, cold). Fourth pillar of the Windows lab family.
- Zero-knowledge course set now: Linux, Apple, Android, Windows, Windows-Malware, GPU, Low-Level/Emulator, Driver, EDR, Auth-Tooling, Potato, NetExec — 12 floors.
- ✅ **POTATO-COURSE.md added**: zero-knowledge gated O0–O12 token-privesc specialization — token anatomy + SeImpersonate lab → named-pipe impersonation → COM-activation seed → spooler→pipe SYSTEM trigger → own PrintSpoofer-class → CLSID-rotation Juicy-class → GodPotato-style duplication study → token-helper lib → vector-patch evolution matrix → Sigma detections of own runs → guarded multi-mode CLI → cold capstone on era-correct isolated Server/Win10 VMs (SYSTEM from LocalService); lab-only hardline incl. non-lab target refusal.
- Zero-knowledge course set now: Linux, Apple, Android, Windows, Windows-Malware, GPU, Low-Level/Emulator, Driver, EDR, Auth-Tooling, Potato — 11 floors.
- ✅ **AUTH-TOOLING-COURSE.md added**: zero-knowledge gated H0–H12 — credential inventory → own NT-hash + offline cracker → hand-built NTLMSSP → hand-built AS-REQ/TGT → own-lab Kerberoast/AS-REP re-creation → own SAM/SYSTEM hive parser → LSASS read attempt (Defender-ON block as the lesson) → DPAPI decrypt of own lab data → raw SMB2 client → MSRPC share enumerator → own psexec-style exec → Sigma detections of H4/H10 → closed-notes capstone (one lab-only tool chaining hash→ticket→crack→SMB→exec). Lab-only hardline incl. fake-creds rule; feeds yet pairs Windows N10/N11 + EDR T7/T8.
- ✅ **EDR-COURSE.md added**: zero-knowledge gated T0–T12 defensive-product build — process watcher (Linux /proc) → shared event schema + normalizer → eBPF exec catcher → own Sigma/YARA engine — macOS collector (EndpointSecurity w/ log-stream+ps-diff fallback) + injection-catch rule → Windows ETW collector (24-119) + loader-duel rule → cross-platform single engine / three feeds → dry-run-gated response actions → 3-round attack/detect/respond duel → closed-notes capstone (one engine catches one technique per OS). Pairs the offensive courses rule-for-rule.
- Zero-knowledge course set now: Linux, Apple, Android, Windows, Windows-Malware, GPU, Low-Level/Emulator, Driver, EDR — 9 floors.
- ✅ **DRIVER-COURSE.md added**: zero-knowledge gated V0–V12 — Linux kernel module→params//proc→chardev→platform/DT→MMIO→DMA→IRQ (all on QEMU's "edu" trainer) → macOS dext + IOKit user client → Windows KMDF service → **same edu device under all three OSes** → fuzz-lite self-security audit → closed-notes capstone with model/attack-surface comparison; feeds DESKTOP-OFFENSIVE-PATH W1 and pairs the kernel-exploit courses as the build side of the same coin.
- ✅ **LOWLEVEL-EMULATOR-COURSE.md added**: zero-knowledge gated B0–B12 (boot-sector hello → fetch/decode/execute + endianness + interrupts → CHIP-8 → 6502 + NES → RISC-V → PS1 MIPS/GTE → software rasterizer → **own PS1 working subset** → PS5 honest study-ceiling roadmap paper (Zen2/RDNA2/Orbis blockers) → closed-notes capstone (second PS1 game or cold 2600-core) ; feeds EMULATOR-VM-PATH E3.
- Platform+domain zero-knowledge course complete set: Linux K, Apple A, Android D, Windows N, Windows-Malware M, GPU U, Low-Level/Emulator B — seven gated floors.
- ✅ **WINDOWS-MALWARE-COURSE.md added**: zero-knowledge gated M1..M12 over the shared N-course floor (F1–F5 = N0–N5 by reference) — loader → persistence → AMSI patch + script-logging evasion → own packer → own beacon+C2 (HTTP/DNS/fallback) → staged exfil → write-your-own Sigma/YARA detections → LOLBin comparison → ransomware-shaped lab (own decryptor) → full two-VM op with cleanup → closed-notes capstone; Defender-ON adversary lab; feeds DESKTOP-OFFENSIVE-PATH W7.
- ✅ **WINDOWS-SECURITY-COURSE.md added**: zero-knowledge gated N0–N12 (hello+PE→token/integrity→PE parser-lite→ntdll/native API→first controlled RIP→GS/ASLR/DEP/CFG tradeoffs→token/service privesc→own KMDF driver→HEVD SYSTEM→ETW self-detection→build own AD domain+Kerberos→Kerberoast/pass-the-hash/golden ticket on own lab→closed-notes capstone), eval-ISO+snapshot lab safety, feeds DESKTOP-OFFENSIVE-PATH W5/W7.
- New zero-knowledge platform courses set complete: **Linux (K0–K12), Apple (A0–A12), Android (D0–D12), Windows (N0–N12)** — four floors, each gated with verification+own-words quizzes and a closed-notes capstone, each feeding its advanced gated path.
- ✅ **LINUX-SECURITY-COURSE.md added**: zero-knowledge gated course K0–K12 (hello in VM→process model→ASLR live→syscalls→ELF loader+LD_PRELOAD→first RIP control→full mitigation bypass chain→own kernel module→Dirty COW/Pipe-class re-creation root→SUID/caps privesc→LSM/AppArmor fences→detection of rootkits→closed-notes capstone), VM-snapshot safety, verification+quiz gates; feeds DESKTOP-OFFENSIVE-PATH W1 + pre-warms Android kernel units.
- Also same-round Apple (A0–A12) and Android (D0–D12) zero-knowledge courses: the 3-course “floor” set — Linux is the shared kernel spine for all platforms.
- ✅ **ANDROID-SECURITY-COURSE.md added**: zero-knowledge gated course D0–D12 (hello APK→signing→APK/DEX smali tamper→Zygote/uid→Binder→sandbox/SELinux→NDK crash→first controlled crash→canary/ASLR bypass→Linux-kernel connect→AVB/dm-verity boot chain→own root-primitive re-creation→closed-notes capstone), emulator-first, verification+quiz gates; feeds JAILBREAK-ROOT-PATH Lane B R4.
- ✅ **APPLE-SECURITY-COURSE.md added**: zero-knowledge gated course A0–A12 (hello→signature→Mach-O→syscall→sandbox→first controlled crash→canary bypass→xv6/XNU kernel-connect→kernelcache→jailbreak-era map→own KFD-class re-creation→closed-notes capstone), verification+lesson-check gates; feeds JAILBREAK-ROOT-PATH Lane A L4.
- ✅ **GPU-BEGINNER-COURSE.md upgraded**: per-unit written lesson checks (own-words quiz gates before advancing), verification-first structure, + new U11 closed-notes capstone (emulator shader pack / compute library / graphics toy) — GPU-PATH S4 entry locked behind capstone pass.
- ✅ **GPU-BEGINNER-COURSE.md added**: zero-knowledge course U0–U10 — Metal mental model, full runnable first kernel (U1), thread/coalescing/shared-memory lessons with benchmark homework, triangle+textures, **emulator scanline/CRT shader milestone (U7)**, profiler discipline, matrix-multiply (LLM-forerunner), then entry into GPU-PATH S4.
- ✅ **GPU-PATH.md added**: gated S1..S8 GPU lane, easiest-first Metal → Vulkan(MoltenVK) → DX12(reading) — arch/SIMD/software-render baseline → Metal compute → emulator shaders (ties into your NES/PS1 cores) → profiling discipline → Vulkan port → DX12 study-note ceiling → LLM/ML compute (Metal MNIST + int8 quantization; CUDA-T4 stays reading per repo rule) → close-notes capstone (path tracer / Metal inference / FFT).
- ✅ **EMULATOR-VM-PATH.md added**: gated E1..E7 machine-building lane — CHIP-8 → NES (6502/PPU/APU/mappers) → GBA/SNES second CPU → **PS1 (MIPS/GTE/raster/GPU/SPU)** → RISC-V + hypervisor/unikernel VM layer → Unicorn/WASM-JIT/gdb-stub/profiler tooling → close-notes capstone (boot + tests + frame rate + one extension); rest-week fun cores (Game Boy/Atari 2600).
- ✅ **DESKTOP-OFFENSIVE-PATH.md added**: gated W1..W8 desktop offensive lane — Linux kernel module→exploit→own-OS+gdb-stub depth (W1–2), macOS userland+kernel-side (W3–4), Windows HEVD/KMDF/ETW (W5–6), malware dev+C2 (W7), rootkit/bootkit+full engagement (W8); own-VM labs, no-skip artifacts, complete the "all platforms offensive" trio with the two mobile docs.
- ✅ **MOBILE-OFFENSIVE-PATH.md added**: the full iOS/Android offensive surface beyond kernel — gated M0..M7 (instrument → app RE → on-device traffic → storage/Keychain → implant/persistence → evasion round-trip vs own detectors → BT/USB/TEE hardware-adjacent → full-stack engagement capstone), same no-skip artifact rules, interleaves with the kernel lanes.
- ✅ **JAILBREAK-ROOT-PATH.md added**: prerequisite-locked dual-lane chain (G0 foundations → iOS L1–8 → Android R1–6) with per-gate artifacts, no-gate-skip rule, own-device/safety guardrails, re-derive-not-copy bar, and an un-packed ~12-month timeline — the gated route through existing steps (04/07/09/24 for iOS; 16/21/17/24 for Android), zero new steps needed.
- ✅ **Odd-silicon era added** (1, niche): `29-07-odd-silicon-era` — the five consoles 29-06's classes skipped (PS2 EE vector units, PS3 Cell SPE + LV0/LV1 HV split, Saturn dual-SH-2, Dreamcast SH-4+Windows CE, Jaguar 4-CPU, Vita ARM): architecture-diff sheet vs your known ISAs, an SPE-style explicit-DMA/SIMD kernel re-created as Metal compute (24-111), and LV-privilege-domain comparison (PS3 vs Xbox vs Switch).
- ✅ **Long-tail console archive added** (1, niche): `29-06-console-archive-long-tail` — the ~40 unstudied families sorted into 6 classes (discrete-logic TV consoles, 6502/68k retro families, media-tray CD consoles, Nintendo 3D+ era, micro/PC handhelds (Ouya/Steam Deck/Legion Go/ROG Ally/MSI VCS), cloud/VR ceilings) with a coverage matrix + Atari 2600 emulator-lite build (6507 = own 6502 delta, TIA chip).
- ✅ **Topic-keyword index added**: `CURRICULUM-1YEAR.md` now maps every discipline keyword (memory corruption → fuzzing → obfuscation → hypervisors → firmware → IR…) to the exact phase/steps teaching it — the full vocabulary list from the request resolves to existing curriculum, no dead rows.
- ✅ **Per-platform low-level telemetry/I-O closed** (2, stretch): `24-118-own-io-uring-lite` (ring-pair SQE/CQE ABI, fixed-file ops, epoll-vs-io_uring ops/s+latency curves, CVE-class edge-case fuzz on own Linux VM), `24-119-own-etw-consumer` (StartTrace/ProcessTrace consumer on own Windows eval VM, own-test-binary detection cross-checked vs logman, ETW-tampering survival analysis — the third row of the cross-OS telemetry chart with 24-103/24-86).
- ✅ **Console platform security added as Phase 29 (Track U, 5 steps, niche/W41+)**: trust-model landscape + entry-surface inventory (`29-01`), PS4/PS5 Orbis FreeBSD chains with own-VM UAF re-creation + hardening loop (`29-02`), Nintendo Switch Tegra/TrustZone/RCM with NCA parser + emulated boot-verifier flip sim (`29-03`), Xbox HV/Secure-Kernel with isolation lab (`29-04`), capstone platform matrix + research-grade deep-dive (`29-05`); own consoles lab-only, every lab runs in own VMs/parsers/emulators — closes the last closed-platform family the curriculum didn't touch.
- ✅ **Game Boy emulator added** (1, stretch): `24-117-own-gameboy-emulator` (LR35902 core + PPU/scanline timing + MBC banking + APU-lite; blargg test-ROM verification; emulator-bug-vs-console-bug writeup; Phase 02 had GBA/SNES/PS1/PSP — the DMG was the gap).
- ✅ **Wireless completed on own Mac** (1, stretch): `24-116-own-ble-scanner` (CoreBluetooth scan + AD parse + GATT walk/read/notify on own device; advertisement-replay spoof demo; Phase-26 attack surface grounded in observed packets).
- ✅ **A/V post + iOS RE added** (3, stretch): `24-113-own-noise-reduction` (STFT + noise-floor profile + spectral gating with attack/release; SNR before/after + spectrogram proof), `24-114-own-image-stabilizer` (feature detection + translation-model motion fit + trajectory smoothing; own shaky clip stabilized with residual curve), `24-115-own-ios-app-analyzer` (zip unpack + Mach-O objc class/method inventory + entitlement decode via own 97/109; verified vs class-dump; the no-device iOS RE entry).
- ✅ **1-year curriculum added**: `CURRICULUM-1YEAR.md` — explicit W1–52 rule (spine-first, 1–2 builds/month), essential-vs-optional matrix per phase, per-platform priority lists (iOS/macOS/kernel/low-level-networking), niche specializations (Asahi Linux bringup method, Hoshino-Lina-style game RE), year-exit test.
- ✅ **Niche/specialist builds added** (3, stretch): `24-110-own-arm64-playground` (mmap RX runner for hand-written A64: ABI/syscalls/PIC adrp dance, clang+15-08 verification, x86-64-vs-arm64 writeup), `24-111-own-metal-compute` (port own 10-15 net to Metal compute kernels on own Mac: buffers/threadgroups, CPU-vs-Metal benchmark table, int8/fp16 quantization on-GPU), `24-112-own-game-cheat-tool` (mach_vm scan/refine/write on own processes/games, pointer-chain find, anti-cheat-detection writeup; pairs 21-10/24-103 skill reuse).
- ✅ **Apple config format added** (1, stretch): `24-109-own-plist-parser` (XML + bplist00 object/offset-table walk, byte-level round-trip vs `plutil`, uid/cycle handling; IPO 24-104 entitlements + mobileprovision CMS unwrap via 24-70; corrupted-offset/cycle-bomb robustness; parser-divergence + XProtect notes).
- ✅ **A/V production + streaming completion added** (3, stretch): `24-106-own-chiptune-sequencer` (MIDI-lite events + pattern/order-list song model, square/pulse voice with slides from own 24-40, 24-14 TUI edit/play; renders master via 24-100), `24-107-own-subtitle-engine` (SRT/VTT cue parse + timing + 24-49 glyph burn-in into own 24-98 exports; malformed-cue robustness sweep; subtitle-attack writeup), `24-108-own-dash-player` (MPD + fMP4 moof/traf/trun sample math, 24-17 fetches, ABR with throughput estimation + rep switching under netem dips; DASH-vs-HLS/CMAF writeup).
- ✅ **Apple OS internals added** (3, stretch): `24-103-own-macos-process-monitor` (sysctl+libproc listing/metrics vs 24-30 sampling, 24-14 TUI; cross-checks top/ps; macOS-vs-Linux visibility + SIP/TCC writeup), `24-104-own-dylib-injector` (probe dylib + interpose, DYLD_INSERT_LIBRARIES + LC_LOAD_DYLIB patch via own 24-97, hardened-runtime/signing wall reproduced with exact errors; loader-security writeup), `24-105-own-logarchive-parser` (tracev3 chunk store + catalog parse + zlib text chunks, timeline reconstruction vs `log` oracle; hunt own 103/104 activity; privacy/tampering DFIR notes).
- ✅ **A/V engineering added** (3, stretch): `24-100-own-audio-editor` (WAV/AIFF I/O, peak-decimation waveform, cut/fade/crossfade + 22-10 biquad EQ/echo chain with A/B; renders a master clip), `24-101-own-video-editor` (clip graph + timeline tracks, scrub via own demuxers, cut/ripple + crossfade, export back through 24-98 with frame-accuracy check), `24-102-own-hls-streamer` (keyframe segmentation + EXT-X playlist + serve over own 24-17; AES-128 EXT-X-KEY-lite; 24-80 flood vs 24-90 limiter smooth-playback stress; HLS-vs-DASH writeup).
- ✅ **Apple formats added** (3, stretch): `24-97-own-mach-o-parser` (fat/LC/dyld-info walk + LC_CODE_SIGNATURE cdHash computed and matched to `codesign`; __TEXT patch lab explains code-sign validation; feeds own 15-08 disassembler), `24-98-own-mp4-muxer` (ISOBMFF box tree + stbl sample-table math + timescale sync; own 24-76 video + 24-40 audio muxed to a QuickTime/VLC-valid file; corrupt stco/robustness lab), `24-99-own-dmg-reader` (UDIF koly/BLKX chunk map + zlib run decompression → raw image mounted with own 24-33 APFS skills; chksum verify catches corruption; quarantine/click-attack writeup).
- ✅ **Formats/client/DNS-integrity added** (3, stretch): `24-94-own-pdf-reader` (object/xref/stream parser + Flate + text ops rendering via 24-73; fuzz-lite robustness runs clean; maldoc/parser-divergence writeup), `24-95-own-imap-client` (tagged/literal grammar, UID/flags, MIME-lite parse, UIDVALIDITY delta sync; full loop against own 24-34 server + interop oracle; STARTTLS-stripping notes), `24-96-own-dnssec-lite` (RFC 4034 canonical RRset signing with own 24-70 keys, DNSKEY/DS trust chain, verifier in own 24-19 resolver; forged/unsigned answers rejected with reasons; signed-zone end-to-end).
- ✅ **Fun+search added** (1, stretch): `24-93-own-sokoban-solver` (state space + admissible-distance A*, corner/corridor deadlock pruning with node counts, IDA* fallback, reverse-pull level generator; solves 3 published boards with heuristic-comparison table).
- ✅ **Defense/supply-chain/signal added** (3, stretch): `24-90-own-rate-limiter` (token bucket/sliding-window/fixed-window middleware in own 24-17, 429+Retry-After; 24-80 flood curves before/after; bypass writeup), `24-91-own-supply-chain-signer` (in-toto-lite layout+link metadata, own 24-70 CA signing, end-to-end verify of own 24-78 pipeline; layer/forge/replay tamper cases rejected), `24-92-own-fft-visualizer` (radix-2 FFT + Hann window from scratch, live bands on own 24-40 synth output; DTMF bins verified exactly; FFT-everywhere notes).
- ✅ **Telemetry/toolchain/directory/voice added** (4, stretch): `24-86-own-htop-lite` (/proc parsing + own 24-14 TUI, cross-checks raw /proc; rootkit-faking visibility writeup), `24-87-own-linker` (ELF symbol resolution + relocations + PLT/GOT-lite; 24-07+24-09 own toolchain produces a runnable binary), `24-88-own-ldap-lite` (BER/ASN.1 bind/search with schema; filter-injection bypass lab + anonymous-listing misconfig; ldapsearch interop), `24-89-own-tts` (Klatt-style formant/glottal-pulse synthesis from own 24-40; prosody + intelligibility ABX; voice-biometric caveats).
- ✅ **Debug/backup/fleet/init added** (4, stretch): `24-82-own-kernel-gdb-stub` (RSP protocol in own 24-01 kernel, gdb attaches via QEMU serial, software/hw breakpoints; debugger-as-attack-surface notes), `24-83-own-backup-tool` (restic-lite: CDC dedupe chunks + AES-GCM snapshot tree, tamper-detected restore; 100-twist dedupe table), `24-84-own-init-system` (unit model + DAG deps + restart policy + cgroups-lite on own 24-13; unit-trust privesc lab + ownership fix), `24-85-own-config-manager` (ansible-lite modules over own 24-12 SSH, check-then-apply idempotence, drift detection; trust-boundary writeup).
- ✅ **ML/defense/OS added** (3, stretch): `24-79-own-malware-ml-detector` (features from own 12-06/27-06 samples → own 10-15 classifier, honest splits/metrics; packing-evasion loop), `24-80-own-packet-replayer` (pcap replay with rate control → regression harness for own 24-57 IDS; capacity curve at 10x), `24-81-own-unikernel` (single-app image on QEMU with own 24-05/24-17; size/boot/attack-surface table vs 24-01 OS).
- ✅ **Codec/audio/containers added** (3, stretch): `24-76-own-video-codec-lite` (intra/delta frames, RLE + block motion search, rate control vs PSNR; corrupt-bitstream clean degrade), `24-77-own-voip` (RTP/RTCP-lite, adaptive jitter buffer, loss concealment; jitter/loss stress curve + security writeup), `24-78-own-container-image-builder` (Dockerfile-lite → OCI layers, content-addressed + manifest; runs in own 24-13 runtime, tamper/hash + cache-staleness lab).
- ✅ **Data/vector/devops/supply-chain added** (4, stretch): `24-72-own-spreadsheet` (formula parser, dependency-graph dirty-recalc, cycle detection; CSV interop), `24-73-own-svg-renderer` (path grammar/curves/transforms → own framebuffer; entity-bomb + malformed clean-failure lab), `24-74-own-ci-runner` (YAML pipelines in own 24-13 containers, artifact flow, secret redaction; malicious-workflow sandbox lab), `24-75-own-package-registry` (npm-lite server + semver resolution + signing; typosquat/prototype-pollution detection lab; interop with own 24-39).
- ✅ **Terminal/identity/crypto-storage added** (4, stretch): `24-68-own-terminal-multiplexer` (tmux-lite on own 24-14: pty panes, scrollback/copy-mode, detach-reattach, resize reflow), `24-69-own-webauthn-authenticator` (FIDO2-lite: CBOR, register/assert, origin binding; replay/origin/phishing lab), `24-70-own-ca` (ASN.1/DER X.509, CSR signing, chains, CRL/OCSP-lite, bad-cert rejection matrix, openssl interop), `24-71-own-encrypted-overlay-fs` (gocryptfs-lite: AES-GCM content + encrypted filenames, FUSE-lite mount, GCM tamper detection, metadata-leak writeup).
- ✅ **DSP/infra/DNS added** (3, stretch): `24-65-own-audio-modem` (text over sound: FSK/DTMF encode with own 24-40 synth, Goertzel demod, CRC framing; speaker↔mic error-rate curve), `24-66-own-load-balancer` (24-17 reverse proxy, least-conn/consistent-hash, health drain + failover matrix; X-Forwarded-For spoof lab), `24-67-own-adblock-dns` (Pi-hole-lite: sinkhole over own 24-19 resolver, blocklist pipeline + validation, per-client metrics → 24-51; DGA/DoH evasion lab).
- ✅ **Network/game/crypto-key added** (3, stretch): `24-62-own-socks-proxy` (SOCKS5 CONNECT/BIND/UDP + HTTP CONNECT, chaining through own 24-18 WG; real-client interop + auth/misconfig lab), `24-63-own-raycaster` (DDA engine from 10-05/10-13 arc, textured + sprites, playable; profile→SIMD FPS table), `24-64-own-hd-wallet` (BIP39 mnemonic → PBKDF2 seed → BIP32 CKD derivation → secp256k1 offline signing, reference-vector verified; weak-entropy + threat-model lab).
- ✅ **Infra/formats/ML/p2p added** (4, stretch): `24-58-own-object-storage` (S3-lite REST over own KV+FS, multipart, sigv4-lite + ACLs; misconfig-abuse lab), `24-59-own-qr-codec` (encode + decode, GF(256) Reed–Solomon with Berlekamp–Massey; damage-correction demo), `24-60-own-spam-filter` (naive Bayes over own 24-34 corpus, precision/recall discipline; adversarial obfuscation loop), `24-61-own-p2p-sync` (Syncthing-lite: version vectors, chunked delta sync over own 24-29 DHT/24-53 traversal; no-silent-loss conflict matrix).
- ✅ **Browser/chain gap closed** (2): `08-05-own-js-engine-exploit` (planted UAF in own 24-47 interpreter → heap feng-shui with own allocator → R/W → exec in own harness; fix diff + why-real-engines-harder), `09-05-own-exploit-chain` (capstone: own JS UAF → own 06-11 sandbox escape → xv6 LPE, primitives + boundary hand-offs instrumentable).
- ✅ **Network tools added** (2, stretch): `24-56-own-network-scanner` (Nmap-lite: SYN/connect scans, port states, banner grab, OS fingerprint-lite; tcpdump-verified vs own VMs), `24-57-own-ids-engine` (Snort-lite: rule language, content-match engine with prefilter, TCP reassembly, alerts → 21-06 pipeline; evasion lab vs own scanner).
- ✅ **Mesh/container/3D added** (3, stretch): `24-53-own-tailscale-lite` (coordination server + STUN-lite hole punch + DERP-lite relay on own 24-24 NAT; p2p-vs-relay capture lab), `24-54-own-video-container` (RIFF/AVI mux+demux, MJPEG + own 24-40 audio, plays in real player; truncation clean-degrade), `24-55-own-3d-model-loader` (OBJ + glTF accessors/scene graph into own 10-05/10-13 renderer; malformed-glTF clean-failure matrix).
- ✅ **ML/storage/distributed added** (3, stretch): `10-16-own-model-quantization` (int8 PTQ pipeline on own 10-15 MNIST, SIMD GEMM + requantization, accuracy/speed table, outlier writeup), `24-51-own-time-series-db` (series/chunks, Gorilla delta-delta + XOR compression, downsampling; ingests own 05-14 eBPF events), `24-52-own-message-queue` (topics/partitions, offsets + at-least-once, consumer groups + rebalance; auth/TLS + poison-message quarantine lab).
- ✅ **Orchestration/formats/hardware added** (3, stretch): `24-48-own-orchestrator` (kube-lite: declarative API + scheduler + health/self-heal on own 24-13 runtime; authn/RBAC + breakout lab), `24-49-own-font-renderer` (sfnt/cmap/glyf parse, Bézier rasterization + anti-aliasing, hinting interpreter stretch — third interpreter; malformed-font clean-failure matrix), `24-50-own-logic-analyzer` (sample dumps → UART/SPI/I2C decoders; ESP32 UART + flash-read → firmware-extraction pipeline writeup).
- ✅ **Windows/IoT/language-runtime added** (3, stretch): `24-45-own-registry-parser` (hive format/nk-vk cells; SAM NT-hash extraction; persistence/userassist/ShimCache forensics lab), `24-46-own-mqtt-broker` (packets/topic-tree wildcards/QoS 0-1-2 state machine, real client interop; topic-enumeration + auth/TLS/ACL lab), `24-47-own-js-interpreter` (lexer/Pratt parser/AST eval, closures + prototypes + mark-sweep GC; benchmark vs Node; V8-divergence writeup).
- ✅ **Hardware-security-interface builds added** (3, stretch): `05-16-own-spectre-poc` (v1: mistrain + speculative read + cache-timing leak of own buffer; lfence/mask fix kills leak; CPU-contract writeup), `20-13-own-cache-side-channel` (Flush+Reload/Prime+Probe vs own non-constant-time compare + AES S-box key-byte recovery; constant-time fix flattens), `24-44-own-tpm-lab` (swtpm PCRs, sealed keys, AIK quote/attestation; boot-chain tamper → attestation fail — ties 24-31/18-04).
- ✅ **Understanding-check capstone added**: `24-43-systems-gauntlet` — monthly blind re-implementation of a random finished Track P step (spec links only, no peeking at own code/); old-vs-new diff + forgot-list debrief; rotate until forgot-list empty.
- ✅ **DSP/data-structures/storage added** (3, stretch): `24-40-own-audio-synthesizer` (oscillators/ADSR/biquad/delay, WAV render, aliasing lesson — pairs 22-10), `24-41-own-search-engine` (inverted index + BM25, index own steps/ tree; SIEM/EAV tie-in), `24-42-own-key-value-cache` (RESP — real redis-cli interop, LRU/LFU eviction, AOF+RDB persistence, kill-9 recovery).
- ✅ **Fun-classic + supply-chain added** (4, stretch): `24-36-own-bittorrent-client` (bencode/tracker/peer wire/rarest-first/tit-for-tat, real legal download, DHT tie-in 24-29), `24-37-own-chess-engine` (bitboards, negamax/alpha-beta, eval PSTs, UCI, NNUE tie-in 10-15), `24-38-own-text-editor` (raw-mode/gap buffer/search/highlight, hostile-file clean-handling lab — terminal-injection from 24-14 other side), `24-39-own-package-manager` (deps/versioning/lockfile, Ed25519-signed manifests; tamper/dependency-confusion/downgrade attack lab — xz-era supply chain).
- ✅ **Crypto/analysis/formats added** (3, stretch): `20-12-own-password-manager` (XChaCha20-Poly1305 vault + Argon2id KDF sweep, zeroization/clipboard; offline-attack lab vs own 20-08 GPU kernels — weak params cracked, strong infeasible), `06-11-own-malware-sandbox` (seccomp + ptrace trace, tmpfs/network shadowing; detonates own 12-06 implant + 27-06 beacon → flagged, benign clean, Sigma export), `24-35-own-jpeg-decoder` (markers/Huffman/reuse 24-25, dequantize + iDCT, 4:2:0 upsampling; malformed-JPEG clean-failure matrix; profile via 24-30).
- ✅ **Forensics formats + mail infra added** (3, stretch): `24-32-own-ntfs-parser` ($MFT record walk, runlists, deleted-record carving, USN journal, ADS hide-file lab, timestamp-semantics writeup), `24-33-own-apfs-parser` (NXSB/object-map, volume B-trees, snapshot walk; snapshot-recovery lab — deleted-by-design recoverable; APFS-vs-NTFS writeup), `24-34-own-mail-server` (SMTP + DKIM signing + SPF/DMARC verify, local only; forged-from spoofing lab → rejected; why spoofing persists).
- ✅ **Popular-OS + hardware-interface builds added** (4, stretch): `04-09-freebsd-deep-dive` (closes last popular-OS gap: jails/ZFS/pf/DTrace; jail-breakout + ZFS-forensics/rollback labs), `22-13-own-rtos-scheduler` (TCBs/priority preemption/semaphores/SysTick-PendSV context switch on QEMU ARM + ESP32; TCB-corruption attack → canaries + MPU), `24-31-own-bootloader` (BIOS real-mode + UEFI PE/COFF paths, long-mode handoff to own 24-01 kernel; bootkit placement + Secure-Boot-style detection), `16-07-tee-op-tee-lab` (OP-TEE on QEMU, own TA via SMC; TA fuzz shows secure-world crash vs Android survival; SEP comparison).
- ✅ **Category-gap fill (6, stretch)** — compression/information-theory, advanced DS, image decoding, distributed systems, performance, quantum: `24-25-own-compression` (Huffman→LZ77→DEFLATE, real gzip interop, decompression-bomb cap lab), `24-26-own-lsm-tree` (memtable skip-list→SST→compaction→bloom; amplification benchmark vs own 24-02 B-tree), `24-27-own-png-decoder` (chunks/zlib/unfilter/Adam7, malformed-PNG clean-failure matrix), `24-28-own-quantum-simulator` (qubits/gates/measurement, Deutsch-Jozsa + Grover(2q), verified vs Qiskit; Shor-vs-RSA tie to 20-09), `24-29-own-kademlia-dht` (XOR routing/k-buckets/lookup; Sybil + eclipse labs on own swarm), `24-30-own-sampling-profiler` (perf-lite: sampling, stack-walk, symbolization, flamegraph; EDR-style stack-walk detection tie-in).
- ✅ **Networking-depth builds added** (4, stretch): `24-21-own-bgp-speaker` (RFC 4271 session + RIB vs FRR/BIRD; own-lab prefix-hijack lab, RPKI/ROA validation blocks), `24-22-own-onion-router` (Tor-lite: 3-hop circuits, layered crypto, hidden-service rendezvous, own VMs; traffic-analysis writeup), `24-23-own-quic-lite` (RFC 9000: connection IDs/migration, streams + flow control, TLS 1.3 0-RTT reuse, loss/recovery vs own 24-05 TCP), `24-24-own-nat-router` (forwarding + conntrack-lite + iptables-lite chains; NAT-traversal + firewall-drop-hunting labs).
- ✅ **More builds added** (4, stretch): `24-18-own-wireguard-lite` (Noise-IK handshake + ChaCha20Poly1305 + tun, replay/MITM lab), `24-19-own-dns-resolver` (RFC 1035 wire + cache; Kaminsky poisoning lab → port randomization/0x20/DNSSEC defense), `24-20-own-packet-sniffer` (raw-socket decode + mini-BPF + pcap writer + crafter: ARP/SYN, own-lab only), `10-15-own-neural-network` (MNIST from scratch no frameworks; FGSM adversarial example vs own gradients + tiny defense).
- ✅ **More software/crypto builds added** (4, stretch): `24-15-own-filesystem` (ext2-lite on raw image, own ls/fsck, deleted-file recovery — DFIR payoff), `24-16-own-memory-allocator` (bump → free-list → bins; heap exploits vs own allocator → hardened, safe-linking-style; glibc cross-ref), `24-17-own-http-server` (RFC 7230 parsing; CL.TE/TE.CL smuggling, CRLF, header-confusion attack lab → fixed), `20-11-own-e2ee-messaging` (X3DH + Double Ratchet, two local peers, compromise simulation: past secret, future recovered).
- ✅ **Software-build steps added** (3, stretch): `24-12-own-ssh-server` (RFC 4251–53 transport/KEX/channels/auth; pre-auth attack pass — KEX downgrade, regreSSHion-shaped; real ssh client as oracle), `24-13-own-container-runtime` (namespaces/cgroups v2/pivot_root/seccomp runc-lite; own escape-attempt matrix; pairs 06-10 seccomp builder + 06 escapes), `24-14-own-terminal-emulator` (pty + CSI/OSC state-machine parser + renderer; escape-injection rig — OSC 52 clipboard exfil blocked; pairs 21-04 log-forensics).
- ✅ **Era-tech DIY steps added** (4, stretch): `02-17-wasm-runtime` (decoder/validator → interpreter → x86-64 JIT, WASI host ABI, break-your-own-sandbox), `20-09-ml-kem-kyber` (FIPS 203 from spec, NIST vectors, openssl interop), `20-10-toy-zk-snark` (R1CS → QAP → KZG → pairing, tampered-proof rejection), `05-14-own-ebpf-tool` (CO-RE tracer feeding 21-06 mini-EDR, C2-beacon alert end-to-end). Declined: blockchain (low syssec value), confidential-computing (HW), LLM agent red-teaming (cloud scope gray).
- ✅ **SQLite file-format step added** (`24-11-own-sqlite-file-format`, stretch). Real .sqlite interop: page-1 header, B-tree leaf/interior, freelist, varint/serial-type records; round-trip vs real sqlite3 as correctness oracle; forensics payoff — own reader parses real Chrome History / app DBs (pairs 21-04, Track H). Complements 24-02 storage engine (format rigor + DFIR hook).
- ✅ **Detection/crypto tool-build steps added** (3, stretch): `21-09-own-yara-engine` (rule compiler + Aho-Corasick matcher, verdict parity vs stock yara on own 12-06/12-10 artifacts), `21-10-own-memory-scanner` (kernel base + PID/module walking from raw dump, field parity vs Volatility, finds 27-06 C2 PID), `20-08-gpu-password-cracker` (PBKDF2/bcrypt kernels CUDA + Metal, benchmark vs hashcat). LLM red-team harness declined — cloud/API scope gray.
- ✅ **Deep-build steps added** (4, stretch): `20-07-own-tls` (TLS 1.3 record layer + handshake, openssl interop + tamper test), `24-09-own-c-compiler` (C subset → x86-64, self-hosting round-trip), `24-10-own-riscv-microkernel` (the vertical: kernel boots on your own 02-11 emulator, built with your own compiler/assembler), `15-09-own-decompiler` (stack recovery + CFG structuring + type inference, decompile own 24-09 functions vs source).
- ✅ **CUDA step added** (`10-14-cuda-ai-compute`, stretch). The era API from a Metal base: thread/block/grid → memory model → warp-shuffle reduction → PyTorch custom kernel (GELU/softmax) in a real training loop. Free Colab T4 for HW; Metal/CUDA comparison table as the concept-transfer deliverable. DX12 stays stretch-only (Windows-gaming niche, no AI value).
- ✅ **GPU rendering steps added** (2, stretch): `02-16-emulator-shaders` (NES/SNES PPU as Metal compute, bit-identical to CPU on test ROMs + CRT shader pack: scanlines/aperture-grille/phosphor) and `10-13-path-tracer` (Metal compute ray tracer: spheres → BVH → materials → denoise, converges to CPU reference). Emulator×GPU crossover + the capstone's opposite.
- ✅ **PSP emulator step added** (`02-15-psp-emulator`, stretch). The single-person console ceiling: Allegrex (MIPS R4000-family, extends the 02-14 R3000 core), VFPU 128-bit vector unit, Media Engine dual-core, closed-doc RE (leaked SDK vs GBATEK era). PS2/GC/PS3/360/PS4/PS5 deliberately out of scope — team-year-class targets, documented in the step.
- ✅ **Console emulation steps added** (3, stretch, Track B): `02-12-snes-emulator` (65C816 extends own 6502 core, Mode 7, SPC700 coprocessor), `02-13-genesis-emulator` (68000 CISC + Z80, planar VDP, FM audio), `02-14-ps1-emulator` (MIPS R3000 delay-slot/coprocessor model, GTE, GPU; MIPS cross-note pairs 22-09 firmware RE). GB skipped as 1-week-easy; N64/PS2 out of scope (3D complexity).
- ✅ **Hardware-emulation steps added** (3, all stretch): `02-10-gba-emulator` (ARM7TDMI ARM+THUMB core, PPU affine/OBJ, APU, cycle timing; AGB test ROMs + commercial game to attract), `02-11-riscv-emulator` (rv32ima from spec, bare-metal boots, GDB stub → debug 22-09-class firmware), `05-13-qemu-device-model` (virtio-class MMIO/IRQ/DMA device + own driver DMA round-trip; fuzz own device with 05-12 fuzzer).
- ✅ **Offensive DIY tool-build steps added** (3, all stretch): `05-12-own-coverage-fuzzer` (forkserver + coverage bitmap + mutator/queue; rediscover a bug libFuzzer found), `12-10-own-packer` (section crypto + stub decryptor + entropy/low-entropy mode, detection round-trip vs own 12-03 checks), `22-12-can-bus-injection` (ESP32 + CAN transceiver, own-car sniff/ID-map/inject; why CAN has no sender auth; own vehicle only). Own-C2 framework skipped — Phase 27 already builds one.
- ✅ **DIY tool-build steps added** (4 steps, all stretch): `15-07-mini-debugger` (ptrace/int3/single-step, reproduces 04-01 overflow under own debugger), `15-08-mini-disassembler` (one arch from the manual, objdump-validated on ≥20 binaries), `12-09-mini-loader` (PE parse → map → imports → relocations → entry + reflective variant; the 12-07 primitive from scratch), `06-10-seccomp-sandbox-builder` (filter DSL → BPF → escape own filter → close hole; both sides of the boundary). Own-hypervisor already existed (06-09). Each has a runnable self-check exit.
- ✅ **DIY radio gadgets step added** (step `22-Embedded-USB/11-diy-radio-gadgets`, W36+ stretch). Build-from-scratch gadgets on boards the plan already buys: sub-GHz keyfob replay (CC1101, OOK demod by hand → fixed vs rolling-code), RFID/NFC cloner (PN532, one crypto-1 round reversed, pairs Track L), ultrasonic covert channel (2×ESP32, FFT decode, pairs 22-10). Each with defender-side detection note. Lab-only.
- ✅ **Embedded signals step added** (step `22-Embedded-USB/10-embedded-signals-dsp`, W36+ stretch). The analog/DSP layer the plan lacked — the skill stack for building capture devices: electret mic + preamp (noise floor measured on scope) → ESP32 I²S ADC → FFT/band-pass DSP (ESP-DSP or hand-rolled) → threshold trigger + log; optical laser-pickup stretch (photodiode + transimpedance amp → vibration demodulation). Covers the three gaps from the eavesdrop-gadget skills audit (analog electronics, DSP, optics-stretch) in one step; lab-only per safety rules.
- ✅ **Unicorn CPU-emulation step added** (step `02-Emulator/09-unicorn-cpu-emulation`, W18+ stretch). From the awesome-emulators sweep: the emulator track built for fidelity — this flips emulation into an instrument. Unicorn Engine (angr's default CPU backend): shellcode tracing, self-decrypting-loop unpacking, angr-under-Unicorn solves, unicorn fuzzing harnesses (cross-ref 15-06, 12-03, 05-11). Emulator-as-tool vs QEMU full-system contrast.
- ✅ **Reference added**: [awesome-emulators (DerekTurtleRoe)](https://github.com/DerekTurtleRoe/awesome-emulators) into LEARNING_PLAN resources + `02-09` links.
- ✅ **Wi-Fi security track added** (phase `28-WiFi-Security`, 6 steps, Track T, W28–40, low priority, checkpoint M28). Wi-Fi was entirely absent while USB (F) and BT (R) had tracks. Monitor mode + one USB adapter is the only hardware cost. Stack + capture (01) → WPA2/WPA3 attacks: PMKID/handshake crack, KRACK, Dragonblood transition-mode downgrade (02) → enterprise: EAP relay (hostapd-wpe/EAPhammer), captive portal, probe-triggered evil twin (03) → rogue-AP red-team hosting: hostapd+dnsmasq+iptables NAT, MITM, Wi-Fi→wired pivot (04) → detection mirror: WIDS/clock-skew classes, own Sigma/Suricata air rules (05) → capstone: full engagement + detection round-trip (M28). Low priority by design — cut before Track T. Own lab airspace only.

- ✅ **Own mini-EDR step added** (step `21-Detection-DFIR/06-own-mini-edr`, W36+ stretch, M29). Track M wrote rules (Sigma/YARA) but rules need telemetry. Inverts Track J's offensive primitives: ETW/kernel-callbacks/minifilter (reuses Track G KMDF), auditd/eBPF, macOS ESF + own YARA memory scanner; feeds it your own Track E/J artifacts — ≥2 OSes must alert, FP-engineered on a clean baseline. The detection-primitives half of the EDR skill set that rules alone don't cover.
- ✅ **Anti-forensics step added** (step `12-Malware-Dev/08-anti-forensics`, W30 stretch). Track E evasion covered memory/EDR only; disk artifacts untouched. Timestomping ($MFT SI vs FN), ShimCache/Amcache/Prefetch/SRUM shimming, event-log gaps, browser history laundering; before/after artifact comparison as the lab. Offensive mirror of Track M 04; closes the red-team loop.
- ✅ **Symbolic execution step added** (step `15-Reverse-Engineering/06-symbolic-execution`, W19–20 parallel, stretch). Plan had fuzzing (05-11) but zero concolic/symbolic. angr (3 challenge solves), Triton (one control-flow-flattened deobfuscation), KLEE vs libFuzzer comparison — the decision rule for symex vs fuzzing vs manual RE.
- ✅ **Network detection step added** (step `21-Detection-DFIR/07-network-detection-zeek`, W36+ stretch). Track M read memory, disk, and logs — not the wire. Zeek scripting + Suricata rules that catch your own Track S C2 beacon and own 28-04 rogue AP; host-vs-network coverage table. Detection engineering (own scripts/signatures), not SIEM ops.
- ✅ **Network recon step added** (step `03-Exploitation-Fundamentals/07-network-recon`, W14–16 parallel). Phase 9's chains assumed recon the plan never taught: nmap/RustScan tiers, Amass/CT logs/passive DNS, Shodan/Censys surface, scan-detection awareness cross-ref 21-07.
- ✅ **Honeypots step added** (step `21-Detection-DFIR/08-honeypots`, W36+ stretch). Third detection pillar after rules (03) and telemetry (06): Kippo/Cowrie + web honeypot, one attacker session mined into Sigma + Zeek/Suricata rules — the intel loop closed.
- ✅ **Embedded firmware exploitation step added** (step `22-Embedded-USB/09-embedded-firmware-exploitation`, W36+ stretch). Everything else targets x86-64/arm64 userspace+kernel; this is the ARM32/MIPS router-class discipline: binwalk extraction → QEMU emulation → ROP chain with no ASLR/NX. Own hardware only.
- ✅ **C2 framework development track added** (phase `27-C2-Framework`, 6 steps, Track S, W30–38, checkpoint M27). The "build your own C2" ask, end to end: architecture + real-C2 dissection → own wire protocol (framing/AES-GCM/key exchange) + HTTP beacon → cross-platform Go agent (in-memory, sleep obfuscation, kill switch) → teamserver + operator UI (multi-operator, audit log) → DNS/SMB-pipe/WebSocket transports + redirectors → capstone full op + detection round-trip (beacon caught by own Sigma/YARA rules, fixed, re-run). Prereq Track E core; feeds Track M.
- ✅ **Bluetooth device development track added** (phase `26-Bluetooth-Devices`, 7 steps, Track R, W36–48, checkpoint M26). BT was entirely absent. Build real devices: stack theory → first BLE peripheral (nRF52840/ESP32-C3), poked from Linux/macOS/Windows/Android → HID-over-GATT gamepad (real hardware for Track G's 14-07 turbo idea) → host-side + HCI/air sniffing → classic A2DP/SPP → pairing/security on own hardware + detection mirror → capstone BLE HID turbo gamepad (hardware turbo, secure pairing, OTA, ≥2 OSes). Low priority, pairs Track G/Phase 7/Track N; feeds Track M BT detection rules.
- ✅ **Gamepad turbo step added to Track G** (`14-Cross-OS-Driver-Dev/07-gamepad-turbo-cross-os`, W36–38; capstone renumbered `07→08`; Track G now 8 steps, W28–41). Attaches code to an *existing* consumer USB controller (Xbox-style HID) and adds a feature it lacks — turbo auto-fire — via per-OS interception + virtual-device injection: Linux hidraw+uinput (kernel HID-driver stretch), Windows Raw Input+ViGEmBus (KMDF HID-filter stretch), macOS IOHIDManager+IOHIDUserDevice (userspace-only by design on Apple Silicon). OS-agnostic turbo state machine (button hold → N Hz synthesized presses) is unit-tested standalone; feeds Track M a virtual-HID detection note.
- ✅ **Applied cryptography track added** (phase `20-Cryptography`, 6 steps, Track L, W14–20, checkpoint M21). Crypto was entirely absent. Covers symmetric/modes with a built-by-hand padding-oracle, RSA/ECC + textbook attacks (incl. ECDSA nonce reuse), hashes/MACs + timing side channels, TLS 1.2/1.3 internals, and OS crypto stores (Keychain/SEP, Keystore/StrongBox, DPAPI/BitLocker, LUKS/TPM). Capstone: build a deliberately-weak scheme and break it (M21). Lands W14–20 alongside Phase 4 kernel reading.
- ✅ **Detection engineering + DFIR track added** (phase `21-Detection-DFIR`, 5 steps, Track M, W30–36, checkpoint M22). The blue-team counterweight to an ~80% offensive plan: Volatility memory forensics, log analysis + the threat-hunting loop, Sigma/YARA as production code, disk/artifact forensics (the Track J persistence list, inverted), and an IR capstone over memory+disk+logs → root cause → detection rule. Uses your own Track E/J artifacts as the lab.
- ✅ **Windows userland exploitation step added** (`11-Windows-Kernel/01-userland-exploit-dev`, W29–30, Track D pre). The one missing discipline: SEH overwrite → egg hunters → Windows ROP → CFG/ACG/CET, on OSED-style binaries in the VM, before the kernel work.
- ✅ **General fuzzing methodology step added** (`05-Linux-Kernel-Exploitation/11-fuzzing-vulnerability-research`, W26–30). libFuzzer/AFL++ basics, crash triage (minimize → dedupe → root-cause), and **syzkaller on XNU promoted out of optional Phase 9 into this step**. Exit: one real bug found + minimized + root-caused.
- ✅ **Practice-platform ladder + CTF cadence added**: `03-Exploitation-Fundamentals/06-practice-platforms` (ROP Emporium all 8 → pwnable.tw → exploit.education, parallel W11–14) plus a one-CTF-per-phase line in Phase 3 — the structured reps between pwn.college steps.
- ✅ **Own-hypervisor stretch added** (`06-Sandbox-Escape/09-own-hypervisor`, W38–39): Hypervisor.framework on Apple Silicon or KVM; makes VM-escape writeups read like your own code review.
- ✅ **Priority + load-control synced into `LEARNING_PLAN.md`**: priority A>C>D>G>H>K>F>E>I>B, ">2 concurrent tracks → defer", Track I + Track B stretch = first cuts, Track J (W44–56) = graduation project, W34–46 flagged as worst squeeze.
- ✅ **Real USB device step added to Track G** (`14-Cross-OS-Driver-Dev/06-usb-device-driver-app`, W37–38): the missing real-hardware link — Track G 02 and Track F 02 both used virtual/emulated devices. Build a Raspberry Pi Pico (TinyUSB) vendor-class device, control it from userspace via IOKit (macOS) + libusb (Linux), SwiftUI control app; optional NES-emulator controller cross-link. Capstone renumbered `06→07` and now targets the real device on all 3 OSes (option d). Track G now W28–41 (7 steps); total ~24 moderate.
- ✅ `LEARNING_PLAN.md` synced with `steps/README.md`: added Phase 6 · Sandbox escape (W34–39), S1/S2 checkpoints, timeline row, renumbered Phases 6→9
- ✅ **Kernel-Exploit-Dojo added** (step `05-10`): practice ladder parallel to Phase 5 — Easy challenges per technique class before each CVE, Medium during, re-derive rule. Rationale: Dojo = realistic *workflow* (unfamiliar modules, discovery, graded repetition), CVE ladder = realistic *bugs*. Both, sequenced: Dojo for fluency, CVEs for fidelity.
- ✅ **Graphics track added** (phase `10-GPU-Compute`, Track C — started Metal-only at 8 steps/W12–20/checkpoint M11, then expanded to 12 steps/W12–28; see the expansion entry below for the details). Why Metal not CUDA: your hardware is Apple Silicon; CUDA needs NVIDIA. Concepts (SIMT, coalescing, occupancy) transfer 1:1 if you ever use CUDA/Vulkan.
- ✅ **Windows kernel track added** (phase `11-Windows-Kernel`, 6 steps, Track D, W30–44, checkpoint M12). Third kernel after Linux/XNU — reuses pool grooming, kernel ROP, patch-diffing. Requires: Windows VM + WinDbg + HEVD; recommended Ghidra fluency from the RE phase.
- ✅ **Malware dev track added** (phase `12-Malware-Dev`, 6 steps, Track E, W18–28, checkpoint M13). Designed detection-first: every offensive step has a defensive mirror (step 05 writes Sigma rules against the user's own implants).
- ✅ **USB security track added** (phase `13-USB-Security`, 6 steps, Track F, W24–32, checkpoint M14). Stack basics → Linux driver dev → attack surface → fuzzing/device emulation → iOS (checkm8) + Windows links. Detection-first capstone: attack + own Sysmon/Sigma rules. Feeds Phase 7 (checkm8 is a bootrom USB bug) and Track D (Windows USB driver bugs).
- ✅ **Cross-OS driver development track added** (phase `14-Cross-OS-Driver-Dev`, 7 steps, Track G, W28–41, checkpoint M15). Write-side of the kernel tracks: Linux char + hardware drivers (extends Phase 4's module step), Windows KMDF/WDM (write-half of Track D's HEVD RE), macOS DriverKit dext (kexts are dead on Apple Silicon). Step 06: real USB device (Pico/TinyUSB) + userspace drivers (IOKit/libusb) + SwiftUI app — the only real-hardware step in the plan. Capstone: same driver on all 3 OSes, driving that real device + a deliberate bug handed to Track D.
- ✅ **Reverse engineering phase added** (phase `15-Reverse-Engineering`, 5 steps, W16–20 parallel, checkpoint M16). The previously-flagged biggest gap: Ghidra/Binary Ninja, XNU kernelcache RE (`kcache_id`/`jtool`/`kerneldiff`), crackmes + Flare-On, patch-diffing CVE workflow. Lands W16–20 (alongside Phase 4 kernel reading) so it feeds Track D (HEVD), Track F (checkm8), Phase 7 (iOS).
- ✅ **Android exploitation track added** (phase `16-Android-Exploitation`, 6 steps, Track H, W34–44, checkpoint M17). Android runs Linux — Phase 5 + Track G skills transfer directly. Security model (sandbox/SELinux/Binder) → app attack surface (Frida/drozer) → Binder + vendor kernel surface (KASAN emulator kernel) → real CVEs re-derived (binder UAF CVE-2019-2215, gpio CVE-2020-0041, media) → rooting/Magisk/bootloader/TrustZone → capstone: chain to root + a detection rule. Own AVD/device only.
- ✅ **Android malware development track added** (phase `17-Android-Malware-Dev`, 6 steps, Track I, W40–48, checkpoint M18). Track E concepts (C2, persistence, evasion) transfer; Android mechanics differ: APK/dex/smali hand-patching, AccessibilityService abuse, banking overlays, Play Protect/MTK evasion, dynamic dex loading. Detection-first capstone: implant + YARA/network rules that catch it. Runs in the AVD only.
- ✅ **Rootkit/bootkit track added** (phase `18-Rootkit-Bootkit`, 7 steps, Track J, W44–56, checkpoint M19). Capstone of the offensive tracks, reusing Windows kernel (D), drivers (G), Android (H), iOS (Phase 7): rootkit principles → Linux LKM/eBPF → Windows driver rootkit (callbacks/DKOM/minifilter) → UEFI bootkit (ESP/NVRAM, BlackLotus-class, OVMF VM) → macOS (kext/dext + ESF) → mobile (Android kernel-module + iOS checkm8 bootkit) → capstone: bootkit→rootkit chain + own detection rules.
- ✅ **Runtime hooking track added** (phase `19-Runtime-Hooking`, 7 steps, Track K, W20–32, checkpoint M20). Hooking is one skill with five mechanisms: Windows (IAT/EAT/inline detours), Linux (LD_PRELOAD/GOT/eBPF uprobes), macOS (dyld interpose/fishhook/ObjC swizzle), iOS (Frida + ElleKit tweak model), Android (LSPosed + native bhook/Dobby). Feeds Track E (AMSI/ETW bypasses) and Track J (kernel-side hooks); capstone: same interception harness on ≥5 platforms + memory-scanner detections. **From-scratch first** — you build the IAT patcher, detour engine, GOT rebinder, interpose/fishhook-equivalent, swizzler, and ARM64 trampoline yourself; MinHook/Frida/LSPosed are cross-checks, not the deliverable. Exception: ART/Java-level (LSPosed) and the eBPF runtime are frameworks — you write what's writable and understand what they patch.
- ✅ **Graphics track expanded** (phase `10-GPU-Compute` now 12 steps, W12–28, Track C; M11 capstone at W28). Promoted off the cut list — you want this. Added: **CPU SIMD** (10-02, SSE/AVX/NEON intrinsics, autovectorization, SoA — Apple Silicon = NEON, x86 VM for AVX), **software renderer from scratch** (10-05, tinyrenderer-style rasterizer in pure C — the "what the GPU hides" step), **shader programming** (10-07, same effect in MSL + GLSL + HLSL), **Vulkan via MoltenVK + DX12 stretch** (10-11, one renderer across three APIs; DX12 on the Track D Windows VM, W30+). Existing Metal steps renumbered 03/04/06/08/09/10. Capstone: pick N-body **or** a 3-way renderer (software + Metal + Vulkan), SIMD baseline. Note: macOS has no native Vulkan — MoltenVK translates it to Metal, which is also the honest lesson in API reality.
- ✅ **Sektor7 malware-dev topics mapped in** (step `12-07-sektor7-advanced-md`, W28–30, Track E; Track E now W18–30). Course → plan: **Essentials** = existing 12-01–05; **Intermediate + Advanced vol.1** = new step built from scratch (reflective PE/DLL + COFF loaders, 32/64-bit migration, hidden data storage, exception-driven control flow, custom RPC-like tasking, userland process hiding via `NtQuerySystemInformation`/`Process32NextW` patches); **Windows Evasion** → 12-03 (sandbox/VM detection, packer pass); **Windows Persistence** 27-method catalog → 12-02 (WMI, COM hijack, IFEO, AppInit); **Privilege Escalation** 20 techniques → Track D `11-01` (non-admin → SYSTEM ladder); **Advanced vol.2** (kernel object manipulation, kernel ETW silencing, RW primitives) already covered by Track D + Track J. M13 capstone unchanged (W26–28). Load: W28–30 lands in the crowded middle — if a week exceeds 2 tracks, defer the advanced step's stretch tasks (32/64-bit migration, COFF loading), never the reflective loader.
- ✅ **MalDev Academy mapped in as reference catalog** (https://maldevacademy.com/, Track E). Course → plan: assembly/RE refresher + PE/COFF → 12-01; injection trinity + persistence + DLL sideload → 12-02; evasion set (AMSI/ETW patches, unhooking, direct/indirect syscalls, call-stack spoofing, string hashing — that's Module 51) → 12-03; C2/implant comms → 12-04; RunPE + reflective loaders + modern anti-EDR (VEH) → 12-07; its capstone → parallel reference for 12-06. Same rule as Sektor7: mapped to topics, not syllabi. Paid course; free taste via the official `MalDev-Academy` GitHub org. Gap: step 12-05 (detection) has no MalDev equivalent — it's offensive-only, keep Sigma/Sysmon as the lens.
- ✅ **Pikuma courses mapped in** (https://pikuma.com/courses, Track B + C). Course → plan: **3D Computer Graphics Programming** (45h — a software renderer in C, the exact step) → Track C `10-05`; **Raycasting Engine Programming with C** → optional warmup for `10-05`; **NES Programming with 6502 Assembly** + **Digital Electronics & Computer Architecture** → Track B `02-02` (learn the console you're emulating + the hardware substrate); **PS1 Programming with MIPS Assembly & C** → Track B `02-08` as the post-NES "complex emulator" stretch — PS1's GPU is a primitive rasterizer you can implement with Track C `10-05` skills, so this is the natural next console after M4. Skipped: **Game Physics Engine** (≈ the N-body capstone already), **C++ 2D Game Engine / Lua / Bash / Atari VCS** (out of journey scope), **Compilers, Interpreters & Formal Languages** (only matters if you dynarec a future 3D-console core — revisit then). Paid per-course (~$50–100, lifetime access, 14-day refund); same rule as Sektor7/MalDev: reference catalog mapped to topics, not syllabi.

⚠️ **Lab discipline** (USB/Windows/malware/Android/rootkit tracks): USB attacks, Windows kernel, and malware work in **isolated VMs only**, never on host or networks you don't own. Windows driver builds stay in the Windows VM (test-signing); the macOS DriverKit dext is userspace and host-safe. **Android exploits/malware run in the AVD emulator or your own unlocked device only** — never on a personal/carrier device; public malware samples are for analysis only. **Rootkits/bootkits live in VMs only — UEFI bootkits are strictly QEMU/OVMF** (real-hardware SPI/NVRAM persistence can brick the machine); the macOS rootkit goes on an Intel VM; mobile variants on your own AVD/device. **iOS hooking demos run on a spare device you own** (sideloaded Frida or a jailbroken spare). Windows kernel exploits = BYOVD-style signed-driver bugs — practicing on your own VMs is fine, weaponizing/red-teaming targets requires authorization. Keep all artifacts in the phase's `labs/` folders, and note the target VM config for reproducibility.

## Material to add (not yet in plan)
> **Status: historical** — items below have since been applied or superseded (see Applied sections; latest round at the bottom)

## Applied — gap audit round 2 (Aug 2026)
- ✅ **Rust for systems programming** (step `04-Real-Kernels-Linux-XNU/08`, W14–20, Track A parallel). Zero Rust coverage existed before (only the word *trust*). Linux `rust/`, Android TEE-in-Rust, Windows Rust adoption make it mandatory reading for syssec. Scope: ownership/borrowing/lifetimes → `unsafe` rules + audit → reimplement one Phase 1/4 kernel-adjacent structure in Rust → read kernel `rust/` subsystem. Exit: compiled kernel-adjacent structure + unsafe audit notes.
- ✅ **JSC/WebKit exploitation step** (step `08-Browser-Exploitation/04`, optional, 47+). Phase 8 was V8/d8-only but Phases 7/9 capstones (Trident, ForcedEntry, Pegasus) are all WebKit/JSC bugs. Scope: JavaScriptCore GC/JIT tiers, one known JSC CVE end-to-end, patch-diff from P0/ZDI advisories. Closes the V8→WebKit gap before the chains phase.
- ✅ **Fault injection / glitching step** (step `22-Embedded-USB/07`, stretch, W36 parallel). The hardware side-channel technique class was entirely absent (only timing side channels in Track L). Scope: voltage/EM glitch basics, one working glitch on owned hardware (chipWhisperer-class or Pico DIY), TEE/firmware bypass theory. Capstone renumbered `07→08` to stay last. First cut if hardware budget is tight.
- ✅ **Career phase** (phase `23-Career`, 4 steps, Track O, post-M23, M24). Zero career content existed — plan stopped at M23 with no job conversion. Scope: 5–8 published writeups/portfolio (M1–M24 mapping), resume + mock interviews (kernel/exploit drills), one real coordinated-disclosure advisory walked end-to-end (own labs as the ethical target), career-launch capstone (M24). Zero load on the study window.
- ⚠️ **Deliberately skipped** (gaps that don't earn their weeks for this goal): cloud/k8s exploitation (container escapes already in Phase 6; add only if target roles are cloud-infra), Intel SGX/TDX TEE depth (SEP/TrustZone covered; revisit if a job needs it), cellular/baseband security (SDR tooling is a separate discipline). Revisit triggers noted — do not add now.

## Applied — round 3: Make-Your-Own phase (Aug 2026)
- ✅ **Phase 24 · Make-Your-Own (Track P, W46+, optional)** — the "build your own X" series grafted in, mapped to security-relevant cores instead of pure-CS filler: `24-01` own x86-64 OS kernel (qemu, Phase 1 hardware side), `24-02` own DB/storage engine (file-format + DFIR angle), `24-03` own git (content addressing), `24-04` own UNIX shell (process semantics), `24-05` own TCP/IP stack (Phase 1/4 net), `24-06` own regex engine (Phase 21 signature matching), `24-07` own x86-64 assembler (mirror of Phase 15 RE), `24-08` micro-transformer (literacy to critique GenAI/deepfake-detector claims — the ALCHERA deck angle). Each step debriefs to a security phase. Optional by design; Track A stays priority.
- ⚠️ **Skipped from the catalog** (don't add): own text editor, own grep/wc/ls, own game engine, own blockchain/crypto-currency (no security-relevant core for this path), own compiler/interpreter (already Phase 15 RE + Phase 24 regex/assembler), own container (already Phase 6), own HTTP server (covered by Phase 24 TCP).
1. **Reverse engineering phase** (biggest gap) — you jump from books (Levin) straight to exploit source. Add after Phase 4 (real kernels) or parallel W16–20:
   - Ghidra (free, since macOS/IDA licensing) + Binary Ninja free tier
   - kernelcache analysis: `kcache_id`, `jtool`, `kerneldiff`, decrypted images
   - Practice: crackmes (crackmes.one), Flare-On, microcorruption (embedded RE)
   - *Practical Binary Analysis* (Andriesse); *Reverse Engineering for Beginners* (Yurichev)
   - Exit: triage one real iOS crash log → root cause chain
2. **Fuzzing + vulnerability research methodology** — ✅ **fully applied**: `05-Linux-Kernel-Exploitation/11-fuzzing-vulnerability-research` (W26–30, parallel with Phase 5 tail) covers libFuzzer/AFL++ basics, crash triage (minimize → dedupe → root-cause), the patch-diff loop, and syzkaller-on-XNU promoted up from Phase 9. Exit: one real bug found + writeup. Track G step 05 keeps the driver-fuzzing angle.
3. **More practice platforms in Phase 4** — ✅ **applied**: `03-Exploitation-Fundamentals/06-practice-platforms` (W11–14, parallel): ROP Emporium all 8 → pwnable.tw → exploit.education.
4. **Patch-diffing / CVE research workflow** — add to Phase 3 or as 1 step in Phase 5:
   - `git log` on stable kernel branches, upstream fix commits
   - Learn to find the bug from the patch alone (one CVE per week)
5. **Track B load check** — W6–10 runs xv6 labs + CHIP-8, W12–16 kernel reading + Metal app. If it crushes you: drop APU (step 04) to stretch, keep CPU+PPU. Emulator goal is the Metal renderer, not audio.
6. **Load control — this plan is ~24 months of full-time work.** W20–40 is the crowded middle (Phase 5 + malware E + USB F + hooking K + graphics C tail + drivers G); W34–46 is the worst single squeeze: iOS (Phase 7) + Windows (Track D) + Android (Track H) + sandbox escapes. Cut line: Track I (Android malware — Track E concepts transfer, least novel skill) and Track B stretch; drop those first if you fall behind. Priority: A (core) > C (graphics — your pick) > D (Windows) > G (feeds D) > H (Android) > K (feeds E) > F > E > I > B. Pick one mobile platform as primary; the other goes secondary (re-run later). Track K's desktop half (W20–27) feeds Track E — keep it; its mobile half (W27–32) can slip if the window is tight. If a week has >2 concurrent tracks, defer, don't grind. Track J (W44–56) is the exception — most prior tracks are done by then; treat it as the graduation project. — ✅ **synced into `LEARNING_PLAN.md`** (new "Load control & priorities" section) so it lives where you'll actually read it.
7. **CTF cadence** — ✅ **applied**: one CTF per phase (~2/year): picoCTF, then HTB / ctftime archive. Note added to Phase 3 in `LEARNING_PLAN.md`.

## Workflow recommendations
- `git init` this folder now — not a repo, so no history/rollback on the plan
- Tick checkboxes the day you finish a step, not at phase end
- One "re-derive" pass per exploit step: close the POC, rewrite from memory (already in exit criteria — actually do it)
- Weekly 30-min review: read next step's README on Sunday, so the week has a target
- Notes rule: if you can't explain it in `notes/` in 5 lines, you haven't finished the step

## Curriculum audit (Aug 2026)
**Verdict: coherent, complete, runnable.** 372 steps / 31 phases (Phase 29 Console added); every phase has numbered build-up → capstone; all new steps carry notes/code/labs dirs.
- **One-year framing stands**: `CURRICULUM-1YEAR.md` marks every phase E/O/S, sets the W1–52 rule (spine-first, 1–2 builds/month), gives per-platform priority lists (iOS security, macOS security, kernel, low-level networking, console/game hardware), names niche specializations (Asahi-Linux-style bringup thinking, Hoshino-Lina-style game RE), and a year-exit test.
- **Console platform axis added** (Phase 29, Track U, 6 steps, own-consoles lab-only): trust-model landscape `29-01` → PS4/PS5 Orbis FreeBSD chains with own-VM UAF re-creation `29-02` → Switch Tegra/TrustZone/RCM with NCA parser + boot-verifier sim `29-03` → Xbox HV/Secure-Kernel isolation lab `29-04` → capstone matrix + deep-dive `29-05` → long-tail archive `29-06` (retro/media-tray/3D-era/handheld/micro-PC classes + own Atari 2600 core) — the last closed-platform family, fully emulated/VM-run where possible, read-research where not.
- **Apple ecosystem is a complete build axis**: Mach-O `97`, DMG `99`, dylib injector `104`, plist `109`, macOS monitor `103`, logarchive `105`, arm64 playground `110`, Metal compute `111`, iOS app analyzer `115`, BLE scanner `116` — plus Phase 07/19/25 research. All run on own macOS hardware/VM, no jailbroken devices.
- **Audio/video is a full sub-track**: codec-lite `76` → AVI `54`/MP4 `98` → audio editor `100` + video editor `101` → HLS `102` + DASH `108` → VoIP `77`, FFT viz `92`, TTS `89`, chiptune `106`, subtitles `107`, noise reduction `113`, image stabilization `114`, Game Boy APU `117` — capture→edit→mux→stream→post stack with ffmpeg/VLC/plutil oracles.
- **Per-platform low-level depth**: Linux (own kernel `24-01`, eBPF `05-14`, io_uring `24-118`, Spectre `05-16`), Windows (HEVD/CEVE `11`, ETW consumer `24-119`, PE/registry parsers), macOS/iOS (above), consoles (Phase 29), embedded (Phase 22), wireless (Phase 26/28/29 BLE/Wi-Fi), GPU (Metal `111` + CUDA `10`).
- **Gap closure totals**: Track P grew 43→119, Track U (console) 7; networking, formats, infra, ML, crypto-key, toolchain, OS, devtools, wireless, per-platform telemetry — Game Boy + Atari 2600 emulation (the Phase-02 family gap).
- **Phase coverage**: 31 phases inventoried — no phase all-consumption; capstone labels verified; foundational/career (00/01/23) intentionally read/lab-based.
- **Mechanical integrity**: link-audit resolves every Prev/Next chain (1 real defect fixed across sessions: Spectre `05-16`); new READMEs carry Obj/Tasks/Resources/Exit-Criteria/Links; original steps' legacy sections untouched.
- **Known ceilings (by design, hardware/cloud-gated)**: SDR/packet-radio, baseband/modem RE, confidential-computing HW, real-cluster kube, cloud SIEM ops; emulator ceiling PSP; CUDA ceiling Colab T4; console exploitation on real hardware (lab-only study + own-VM re-creation instead); jailbroken-device-only iOS internals.
- **Improvement if returning**: run top-priority steps end-to-end, empirical durations, re-audit chains after any renumber (numbers are stable IDs).

## Timeline sanity
- Total ~15–19 months is honest for jailbreak literacy. Browser phase (47+) is open-ended by design — don't schedule a finish date for it.
- If motivation dips mid-Phase 2 (xv6), switch to Track B for a week instead of quitting — the parallel track exists exactly for that.
